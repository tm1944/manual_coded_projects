[project]
name = "fast-task-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tallalmohar", email = "tallalmoohar@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydantic>=2.13.5",
]

[project.scripts]
fast-task-api = "fast_task_api:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
